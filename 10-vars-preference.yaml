- name: vars preferences
  hosts: local
  connection: local
  vars:
    NAME: from play level
  vars_files:
  - course.yaml  
  vars_prompt:
  - name: NAME
    prompt: What is your NAME?
    private: false
  tasks:
  - name: testing variables preferences
    vars:
      NAME: from task level
    ansible.builtin.debug:
      msg: "Nmae: {{ NAME }}"

# 1. args or command line 
# 2. from prompt 
# 3. files
# 4. prompt
# 5. play level
# 6. inventory
#